apiVersion: apps/v1
kind: Deployment

metadata:
  name: frontend
  namespace: gram-thread

spec:
  replicas: 1

  selector:
    matchLabels:
      app: frontend

  template:
    metadata:
      labels:
        app: frontend

    spec:
      containers:
        - name: frontend
          image: gram-thread-frontend:v3
          imagePullPolicy: Never

          ports:
            - containerPort: 3000
